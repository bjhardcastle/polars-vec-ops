# This file is based on the autogenerated one by maturin v1.7.8 with:
#
#    maturin generate-ci github
#
# Differences are:
# - removed x86, armv7, s390x, ppc64le targets from Linux
# - removed free-threaded wheels
# - removed musllinux
# - have separate linux-just-test and linux-min-versions-just-test jobs
# - add the `RUSTFLAGS: "-Dwarnings"` env variable


name: CI

on:
  push:
    branches:
      - main
      - master
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

# Make sure CI fails on all warnings, including Clippy lints
env:
  RUSTFLAGS: "-Dwarnings"

jobs:
  linux-just-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64]
        python-version: ["3.9", "3.11", "3.13"]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Rust
        run: rustup show
      - uses: mozilla-actions/sccache-action@v0.0.6
      - run: make venv
      - run: make pre-commit
      - run: make install
      - run: make test

  linux-min-versions-just-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64]
        python-version: ["3.9"]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Rust
        run: rustup show
      - uses: mozilla-actions/sccache-action@v0.0.6
      - run: make venv
      - run: .venv/bin/python -m pip install polars==1.8.0  # min version
      - run: make install
      - run: make test

  # Compute the next version without committing anything.
  # Build jobs apply the same bump locally so wheels carry the right version.
  # The version is only committed to main if all jobs succeed (in `release`).
  compute-version:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    outputs:
      current_version: ${{ steps.current-version.outputs.version }}
      new_version: ${{ steps.new-version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Get current version
        id: current-version
        run: |
          VERSION=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $VERSION"

      - name: Bump version locally (no commit)
        shell: bash
        run: |
          python3 - << 'EOF'
          import re, pathlib
          f = pathlib.Path('Cargo.toml')
          c = f.read_text()
          c = re.sub(r'^version = "(\d+\.\d+\.)(\d+)"', lambda m: f'version = "{m.group(1)}{int(m.group(2))+1}"', c, count=1, flags=re.MULTILINE)
          f.write_text(c)
          EOF

      - name: Get new version
        id: new-version
        run: |
          VERSION=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "New version: $VERSION"

  linux:
    needs: [compute-version, linux-just-test, linux-min-versions-just-test]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
          - runner: ubuntu-22.04
            target: aarch64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Set new version in Cargo.toml
        shell: bash
        run: |
          python3 - << 'EOF'
          import re, pathlib
          f = pathlib.Path('Cargo.toml')
          c = f.read_text()
          c = re.sub(r'^version = "(\d+\.\d+\.)(\d+)"', lambda m: f'version = "{m.group(1)}{int(m.group(2))+1}"', c, count=1, flags=re.MULTILINE)
          f.write_text(c)
          EOF
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: 'true'
          manylinux: '2_17'
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.platform.target }}
          path: dist

  windows:
    needs: [compute-version, linux-just-test, linux-min-versions-just-test]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
          architecture: ${{ matrix.platform.target }}
      - name: Set new version in Cargo.toml
        shell: bash
        run: |
          python3 - << 'EOF'
          import re, pathlib
          f = pathlib.Path('Cargo.toml')
          c = f.read_text()
          c = re.sub(r'^version = "(\d+\.\d+\.)(\d+)"', lambda m: f'version = "{m.group(1)}{int(m.group(2))+1}"', c, count=1, flags=re.MULTILINE)
          f.write_text(c)
          EOF
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: 'true'
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: dist

  macos:
    needs: [compute-version, linux-just-test, linux-min-versions-just-test]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: macos-14
            target: aarch64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Set new version in Cargo.toml
        shell: bash
        run: |
          python3 - << 'EOF'
          import re, pathlib
          f = pathlib.Path('Cargo.toml')
          c = f.read_text()
          c = re.sub(r'^version = "(\d+\.\d+\.)(\d+)"', lambda m: f'version = "{m.group(1)}{int(m.group(2))+1}"', c, count=1, flags=re.MULTILINE)
          f.write_text(c)
          EOF
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: 'true'
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: dist

  sdist:
    needs: [compute-version, linux-just-test, linux-min-versions-just-test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set new version in Cargo.toml
        shell: bash
        run: |
          python3 - << 'EOF'
          import re, pathlib
          f = pathlib.Path('Cargo.toml')
          c = f.read_text()
          c = re.sub(r'^version = "(\d+\.\d+\.)(\d+)"', lambda m: f'version = "{m.group(1)}{int(m.group(2))+1}"', c, count=1, flags=re.MULTILINE)
          f.write_text(c)
          EOF
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  release:
    name: Release
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    needs: [compute-version, linux, windows, macos, sdist, linux-just-test, linux-min-versions-just-test]
    permissions:
      # Use to sign the release artifacts
      id-token: write
      # Used to upload release artifacts
      contents: write
      # Used to generate artifact attestation
      attestations: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Rust
        run: rustup show

      - name: Bump version in Cargo.toml
        shell: bash
        run: |
          python3 - << 'EOF'
          import re, pathlib
          f = pathlib.Path('Cargo.toml')
          c = f.read_text()
          c = re.sub(r'^version = "(\d+\.\d+\.)(\d+)"', lambda m: f'version = "{m.group(1)}{int(m.group(2))+1}"', c, count=1, flags=re.MULTILINE)
          f.write_text(c)
          EOF

      - name: Update Cargo.lock
        run: cargo update --workspace

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Cargo.toml Cargo.lock
          git commit -m "Bump version: ${{ needs.compute-version.outputs.current_version }} -> ${{ needs.compute-version.outputs.new_version }} [skip actions]"

      - name: Create tag
        run: git tag v${{ needs.compute-version.outputs.new_version }}

      - uses: actions/download-artifact@v4

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: 'wheels-*/*'

      - name: Publish to PyPI
        uses: PyO3/maturin-action@v1
        with:
          command: upload
          args: --non-interactive --skip-existing wheels-*/*

      - name: Push changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          tags: true
          branch: main
